name: 'Invalidate Cloudfront'
description: 'Invalidate Cloudfront distribution paths'
author: 'Pixelcop Research, Inc.'
branding:
  icon: 'cloud-lightning'
  color: 'orange'

runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        $GITHUB_ACTION_PATH/entrypoint.sh
